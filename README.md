# Cognito OIDC Router

This is a biolerplate sample application that can be used for a single web application that requires the usage of multiple Amazon Cognito user pools, across mulitple app clients, and even supporting multiple 3rd party IdPs (e.g. SAML or OIDC).

